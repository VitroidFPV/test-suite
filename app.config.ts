export default defineAppConfig({
	ui: {
		primary: "brand",
		gray: "neutral",
		strategy: "override",
		button: {
			color: {
				primary: {
					solid: "bg-primary-500 hover:bg-primary-400 text-black"
				}
			}
		}
	}
})
